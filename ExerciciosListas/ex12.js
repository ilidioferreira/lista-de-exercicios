// Foram anotadas as idades e alturas de 30 alunos. 
// Faça um Programa que determine quantos alunos com mais de 13 anos possuem altura inferior à média de altura desses alunos.

function getAverage (age,height) {
    const heigh
    for (let i = 0; i < 30; i++)
        if(age >= 30 && heigh)
}
